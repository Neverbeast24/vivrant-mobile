<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may talk to LAN IPs over HTTP for local API development. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

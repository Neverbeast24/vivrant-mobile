/// Training hub — directory for daily activity and gym.
///
/// Workout logging lives in `features/movement`. Gym catalog, sessions,
/// and programs live in `features/gym`.
library;

export 'presentation/screens/training_hub_screen.dart';
